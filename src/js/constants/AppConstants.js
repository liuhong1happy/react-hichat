var keyMirror = require('keymirror');

module.exports = {
      ActionTypes: keyMirror({
            POSTED_USER_LOGIN:null
      }),
      EventTypes:keyMirror({
            POSTED_USER_LOGIN:null
      })
};
